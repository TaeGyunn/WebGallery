spring:
  mvc:
    view:
      suffix: .html

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #          show_sql : true
        format_sql: true

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type : trace

file:
  upload:
    location: /Users/taeku/Desktop/images/
